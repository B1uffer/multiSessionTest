server:
  servlet:
    session:
      timeout: 30m

spring:
  application:
    name: multiSessionTest
  h2:
    console:
      enabled: true
      path: /h2-console
  datasource:
    url: jdbc:h2:mem:multiSessionTest
    driver-class-name: org.h2.Driver
    username: sa
    password:
